# 强制脚本使用 LF 换行符。
# CentOS7 上若 Jenkins 的 git 设了 core.autocrlf=true，会把 LF 转成 CRLF，
# 导致 bash 执行 deploy.sh 时解析错乱（报 "-E: command not found" 等）。
# 加 eol=lf 可覆盖 core.autocrlf，保证 checkout 到 Linux 一定是 LF。
* text=auto
*.sh text eol=lf
*.py text eol=lf
Jenkinsfile text eol=lf
deploy.sh text eol=lf
